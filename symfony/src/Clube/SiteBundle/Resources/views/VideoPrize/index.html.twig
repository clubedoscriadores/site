{% extends '::base.html.twig' %}

{% block body -%}
    <h1>VideoPrize list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prizeamount</th>
                <th>Prizeplace</th>
                <th>Createdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('premiacao_video_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.prizeAmount }}</td>
                <td>{{ entity.prizePlace }}</td>
                <td>{% if entity.createDate %}{{ entity.createDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('premiacao_video_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('premiacao_video_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('premiacao_video_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
