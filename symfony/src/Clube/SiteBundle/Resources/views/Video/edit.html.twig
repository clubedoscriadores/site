{% extends 'SiteBundle::layout.html.twig' %}
{% block content %}
    <form action="{{ path('video_edit', {'id':entity.id}) }}" {{ form_enctype(edit_form) }} method="POST" class="cc-register-form cc-contact">
        <input type="hidden" name="_method" value="PUT" />
        <h1 class="cc-register-title">{{ 'Envie seu vídeo' }}</h1>
        <div class="cc-register-wrapper">
            <div class="cc-errors">
                {{ form_errors(edit_form) }}
            </div>

            <div class="cc-form-input">
                {{ form_label(edit_form.title) }}
                {{ form_widget(edit_form.title) }}
                {{ form_errors(edit_form.title) }}
            </div>

            <div class="cc-form-input">
                {{ form_label(edit_form.file) }}
                {{ form_widget(edit_form.file) }}
                {{ form_errors(edit_form.file) }}
            </div>

            <div class="cc-form-input">
                {{ form_label(edit_form.videoPrize) }}
                {{ form_widget(edit_form.videoPrize) }}
                {{ form_errors(edit_form.videoPrize) }}
            </div>

            {{ form_widget(edit_form.project) }}
            {{ form_widget(edit_form.user) }}


            {{ form_widget(edit_form._token) }}

            <div class="cc-form-submit">
                <button class="cc-button">Enviar</button>
            </div>

        </div>
    </form>
    {{ form(delete_form) }}
{% endblock %}